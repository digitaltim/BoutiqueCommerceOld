	<header>
		<nav>
			<li><a href="{{ path_for('home')}}">BoutiqueCommerce</a></li>
	{% if not authentication.check %}
			<li><a href="{{ path_for('authentication.login')}}">Login</a></li>
    {% else %}
            <li><a href="{{ path_for('authentication.login') }}">Admin</a></li>
	{% endif %}
		</nav>
	</header>
